#!/usr/bin/env nix-shell
#!nix-shell -p bash fzf ripgrep -i bash

(
    set -euo pipefail
    cd $PASSWORD_STORE_DIR/Password
    file=$(rg --files | $FUZZY)
    gpg --quiet --decrypt $file | head -n 1 | pbcopy
)
