[user]
name = "marin"
email = "git+jj@mar.in"

[ui]
default-command = "log"
pager = [ "diffr" ]

[ui.diff]
format = "git"

[template-aliases]
# Username part of the email address
'format_short_id(id)' = 'id.shortest()'

[aliases]
la = [ "log", "-r", "::" ]
